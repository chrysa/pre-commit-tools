---
name: pre-commit

on:
    pull_request:
        branches: [main, develop]
    push:
        branches: [main, develop, "feat/**", "feature/**", "fix/**"]

jobs:
    call:
        uses: chrysa/github-actions/.github/workflows/pre-commit.yml@main
        secrets: inherit
