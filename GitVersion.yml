branches:
  feat:
    increment: Minor
    regex: ^feat/.*$
  fix:
    increment: Patch
    label: fix
    regex: ^fix[/-]
    source-branches:
    - main
  release:
    increment: Patch
    label: rc
    regex: ^release[/-]
    source-branches:
    - main
workflow: GitHubFlow/v1
